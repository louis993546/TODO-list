createTable:
CREATE TABLE todoEntity(
    id          TEXT    NOT NULL    PRIMARY KEY,
    title       TEXT    NOT NULL,
    state       TEXT    NOT NULL,
    FOREIGN KEY (state) REFERENCES todoStateEntity(name)
);

insertOne:
INSERT INTO todoEntity(id, title, state) VALUES (?, ?, ?);

selectAll:
SELECT * FROM todoEntity;

-- updateOne: TODO

deleteOne:
DELETE FROM todoEntity WHERE id=?;